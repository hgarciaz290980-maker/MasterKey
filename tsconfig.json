{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    // CLAVE: Esto le dice a TypeScript dónde buscar las rutas absolutas (@/)
    "baseUrl": ".",
    "paths": {
      "@/*": [
        "./*"
      ]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts"
  ]
}